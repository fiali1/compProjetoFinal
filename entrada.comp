inicio
    Numero a, b;
    Texto c;

    ler(a);
    ler(b);
    ler(c);

    a = 10 + 2;

    se (a < b) {
        faca {
            se (a > b) {
                escrever(a);
            }
        } enquanto (a < 1);
    }
fim;