inicio
    Numero a, b;
    Texto c;

    ler(a);
    ler(b);
    ler(c);

    c = "Teste" + "Teste" + "Teste" + "Teste" + "Teste";

    se (a > b) {
        escrever(c);
    }
    senao {
        escrever(c);
    }

    escrever(a);
    escrever(b);
fim;