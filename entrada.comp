inicio
    Inteiro a, b;
    Fracionado d, e, g;
    Texto c;
    a = 11;
    b = 100;
    d = 10.1;

    enquanto(a > b) {
        escrever(a);
    }
    faca {
        escrever(b);
    } enquanto(a < b);

fim;