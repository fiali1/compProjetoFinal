inicio
    Numero a, b;
    Texto c;

    ler(a);
    ler(b);
    ler(c);

    se (c > 0) {
        escrever(a);
    } senao {
        escrever(b);
    }

    se (a > 2) {
        escrever(b);
    }
fim;