inicio
    Numero a, b;
    Texto c;

    ler(a);
    ler(b);
    ler(c);

    a = b;

    se (a > b) {
        escrever(c);
    }
    senao {
        escrever(c);
    }

    escrever(a);
    escrever(b);
fim;